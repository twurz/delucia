require 'spec_helper'

describe Calendar do
  it "can create a session" do
    expect(monday_expr).to exist
  end
  it "is invalid without a name"

  it "is invalid without an email"
  it "is invalid without a phone number"
end
